# VitePress variant

This folder contains a prebuilt static AI-tools listing variant for **VitePress**.

- Generator: `VitePress`
- Jamstack source: https://jamstack.org/generators/
- Render service name: `taaft-vitepress`
- Static output path: `dist/`

Notes:

- Typical static build: vitepress build docs producing docs/.vitepress/dist/.
- The current batch uses shared TAAFT-derived content and a prebuilt static output so each variant can deploy cleanly to Render from a single monorepo.
