# Next.js variant

This folder contains a prebuilt static AI-tools listing variant for **Next.js**.

- Generator: `Next.js`
- Jamstack source: https://jamstack.org/generators/
- Render service name: `taaft-nextjs`
- Static output path: `dist/`

Notes:

- Typical static build: next build with output export or next export depending on app structure.
- The current batch uses shared TAAFT-derived content and a prebuilt static output so each variant can deploy cleanly to Render from a single monorepo.
